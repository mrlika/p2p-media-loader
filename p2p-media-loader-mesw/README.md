# P2P Media Loader - integration on top of HTMLMediaElement using Service Workers

P2P sharing of segmented media streams (currently HLS only) using WebRTC on top of HTMLMediaElement using Service Workers
